# "bare" repositories

> `bare` repositories are here

## Structure

```
└── user_name
    ├── repo-1
    └── repo-2
```

## Initialize a shared repository

```
./init-bare-repo.sh user_name repository_name
```
